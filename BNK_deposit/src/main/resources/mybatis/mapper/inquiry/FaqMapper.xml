<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bnk.dao.inquiry.IFaqDao">
	
	<select id="getFAQ" resultType="com.example.bnk.dto.inquiry.FaqDto">
		SELECT * FROM TB_FAQ
	</select>
	
	<insert id="insertNewFaq">
		INSERT INTO TB_FAQ (
	        FAQ_CATEGORY,
	        FAQ_QUESTION,
	        FAQ_ANSWER,
	        FAQ_ORDER,
	        CREATED_AT,
	        UPDATED_AT,
	        CREATED_BY
	    ) VALUES (
	        #{faqCategory},
	        #{faqQuestion},
	        #{faqAnswer},
	        #{faqOrder},
	        SYSDATE,
	        SYSDATE,
	        #{createdBy}
	    )
	</insert>
	
	
	
</mapper>